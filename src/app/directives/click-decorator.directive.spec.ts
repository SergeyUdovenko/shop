import { ClickDecoratorDirective } from './click-decorator.directive';

describe('ClickDecoratorDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickDecoratorDirective();
    expect(directive).toBeTruthy();
  });
});
